define( [ 'hbs!/templates/loader', 'jquery',  ], function ( one, $ ) {

	'use strict';

   	var Loader = {
		init: function() {

			//document.body.innerHTML = tmplOne( { adjective: "favorite" } );

		}
	};

	return {
		init: function () {
	    	Loader.init();
	    }
	};

} );